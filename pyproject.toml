[project]
name = "python-actor-theater-3000"
version = "0.1.0"
description = "Python actor system using concurrent.interpreters"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0"
]
